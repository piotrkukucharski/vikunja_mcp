name: Build & Package Container

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-go:
    name: Build & Test Go Application
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true

      - name: Install Dependencies
        run: go mod download

      - name: Run Unit Tests
        run: go test -v ./...

      - name: Compile App
        run: CGO_ENABLED=0 GOOS=linux go build -v -o vikunja_mcp main.go

  build-docker:
    name: Build, Save & Publish Docker Image
    needs: build-go
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & Load Docker Image Locally
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: vikunja-mcp:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Save Docker Image as Tarball Artifact
        run: |
          mkdir -p /tmp/artifacts
          docker save -o /tmp/artifacts/vikunja_mcp.tar vikunja-mcp:latest

      - name: Upload Container Artifact
        uses: actions/upload-artifact@v4
        with:
          name: vikunja-mcp-container
          path: /tmp/artifacts/vikunja_mcp.tar
          retention-days: 7

      - name: Log in to GitHub Container Registry (GHCR)
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Metadata for GHCR
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=sha,format=short
            latest

      - name: Build and Push Docker Image to GHCR
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
